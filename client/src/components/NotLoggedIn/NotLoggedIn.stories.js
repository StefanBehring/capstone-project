import NotLoggedIn from './NotLoggedIn'

export default {
  title: 'Component/NotLoggedIn',
  component: NotLoggedIn,
}

const Template = () => <NotLoggedIn />

export const NotLoggedInComplete = Template.bind({})
