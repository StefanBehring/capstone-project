import LoadingSpinner from './LoadingSpinner'

export default {
  title: 'Component/Messages/LoadingSpinner',
  component: LoadingSpinner,
}

const Template = () => <LoadingSpinner />

export const LoadingSpinnerComplete = Template.bind({})
