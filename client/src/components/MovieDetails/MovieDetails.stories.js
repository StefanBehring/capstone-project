import MovieDetails from './MovieDetails'

export default {
  title: 'Component/MovieDetails',
  component: MovieDetails,
}

const Template = () => <MovieDetails />

export const MovieDetailsComplete = Template.bind({})
